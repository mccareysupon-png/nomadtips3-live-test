(()=>{
'use strict';
const VERSION='343-full-odds-bulk-v1-zero-click-requests';
const BOOKS=[
  {slug:'bet365',name:'Bet365',role:'MAIN'},
  {slug:'pinnacle',name:'Pinnacle',role:'REFEREE'},
  {slug:'crown',name:'Crown',role:'REFEREE ASIA'},
  {slug:'1xbet',name:'1xBet',role:'GLOBAL MASS'},
  {slug:'12bet',name:'12Bet',role:'ASIA'},
  {slug:'interwetten',name:'Interwetten',role:'EUROPE'},
  {slug:'macauslot',name:'Macau Slot',role:'EAST ASIA'},
  {slug:'18bet',name:'18Bet',role:'ASIA #2'},
  {slug:'vcbet',name:'VCBet',role:'RESERVE'},
  {slug:'easybets',name:'Easybets',role:'RESERVE'}
];
const LABELS={'asian_handicap':'Asian Handicap · Full Time','goal_line':'Goals O/U · Full Time','1x2':'1X2 · Full Time','corner_line':'Corners O/U · Full Time','corner_asian':'Corner Asian Handicap','card_line':'Cards O/U · Full Time','card_asian':'Card Asian Handicap','btts':'Both Teams To Score','asian_handicap_half':'Asian Handicap · 1st Half','goal_line_half':'Goals O/U · 1st Half','1x2_half':'1X2 · 1st Half','corner_line_half':'Corners O/U · 1st Half','corner_asian_half':'Corner Asian Handicap · 1st Half','card_line_half':'Cards O/U · 1st Half','card_asian_half':'Card Asian Handicap · 1st Half'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>String(v??'').toLowerCase().replace(/[\s_-]/g,'');
const num=v=>v===null||v===undefined||v===''||typeof v==='boolean'||!Number.isFinite(Number(v))?null:Number(v);
const fmt=v=>{const n=num(v);if(n===null)return v===null||v===undefined||v===''?'—':String(v);return Number.isInteger(n)?String(n):String(Math.round(n*1000)/1000)};
const signed=v=>{const n=num(v);if(n!==null){if(Object.is(n,-0)||n===0)return'0';return n>0?`+${fmt(n)}`:fmt(n)}const s=String(v??'').trim();return s||'—'};
function invertLine(v){const n=num(v);if(n!==null)return n===0?0:-n;const s=String(v??'').trim();if(!s)return null;if(s.includes('/'))return s.split('/').map(x=>{const q=num(x.trim());return q===null?x.trim():signed(-q)}).join('/');return s.startsWith('+')?`-${s.slice(1)}`:s.startsWith('-')?`+${s.slice(1)}`:s}
function team(card,side){const names=card.querySelectorAll('.team-name');return side==='home'?(names[0]?.textContent?.trim()||'HOME'):(names[1]?.textContent?.trim()||'AWAY')}
function looksLikeMarketRoot(v){return Boolean(v)&&typeof v==='object'&&!Array.isArray(v)&&Object.keys(v).some(k=>/1x2|asian|goal|corner|card|btts/i.test(k))}
function bookPayloadRoot(payload){if(!payload||typeof payload!=='object')return null;return payload.providerOdds??payload.fullOdds??payload.data??payload}
function providerBooks(payload){const full=bookPayloadRoot(payload);if(!full||typeof full!=='object')return[];const candidates=[full?.data?.bookmakers,full?.bookmakers,payload?.bookmakers,payload?.data?.bookmakers];let raw=candidates.find(Array.isArray)||[];if(!raw.length){const roots=[full?.data,full,payload?.data,payload].filter(x=>x&&typeof x==='object'&&!Array.isArray(x));for(const root of roots){const keyed=[];for(const book of BOOKS){const hit=root[book.slug]??root[book.name]??root[book.name.toLowerCase()];if(hit&&typeof hit==='object')keyed.push({slug:book.slug,name:book.name,...hit})}if(keyed.length){raw=keyed;break}}}if(!raw.length&&looksLikeMarketRoot(full))raw=[{slug:'bet365',name:'Bet365',odds:full}];if(!raw.length&&full?.odds&&looksLikeMarketRoot(full.odds))raw=[{slug:'bet365',name:'Bet365',odds:full.odds}];if(!raw.length&&full?.markets&&looksLikeMarketRoot(full.markets))raw=[{slug:'bet365',name:'Bet365',odds:full.markets}];const found=[];for(const book of BOOKS){const target=norm(book.slug),row=raw.find(x=>{const key=norm(x?.slug??x?.bookmaker?.slug??x?.name??x?.bookmaker?.name);return key===target||key.includes(target)||target.includes(key)});if(!row)continue;const odds=row?.odds??row?.markets??row?.data?.odds??row?.data?.markets??row;if(odds&&typeof odds==='object'&&!Array.isArray(odds))found.push({...book,odds})}return found}
function canonicalMarket(key){const k=String(key??'').toLowerCase().replace(/[\s-]+/g,'_'),half=/(half|1st)/.test(k);if(k==='corner_asian'||k==='corners_asian'||k==='corner_handicap'||k==='corner_asian_handicap')return half?'corner_asian_half':'corner_asian';if(k==='card_asian'||k==='cards_asian'||k==='card_handicap'||k==='card_asian_handicap')return half?'card_asian_half':'card_asian';if(k.startsWith('corner')&&!k.includes('asian'))return half?'corner_line_half':'corner_line';if((k.startsWith('card')||k.startsWith('cards'))&&!k.includes('asian'))return half?'card_line_half':'card_line';if(k.startsWith('1x2'))return half?'1x2_half':'1x2';if(k==='asian'||k.startsWith('asian_handicap')||k==='handicap')return half?'asian_handicap_half':'asian_handicap';if(k.startsWith('goal')||k==='goalline'||k==='total_goals'||k==='goals_over_under')return half?'goal_line_half':'goal_line';if(k==='btts'||k.includes('both_teams_to_score'))return'btts';return k}
function marketKind(key){if(key==='btts')return'BTTS';if(key.startsWith('1x2'))return'1X2';if(key.includes('asian'))return'AH';if(key.includes('goal_line')||key.includes('corner_line')||key.includes('card_line'))return'TOTAL';return'UNKNOWN'}
function rank(key){const half=key.includes('_half');if(key==='asian_handicap')return 0;if(key==='goal_line')return 10;if(key==='1x2')return 20;if(key==='corner_line')return 30;if(key==='corner_asian')return 31;if(key==='card_line')return 40;if(key==='card_asian')return 41;if(half){const bare=key.replace('_half','');return 50+['asian_handicap','goal_line','1x2','corner_line','corner_asian','card_line','card_asian'].indexOf(bare)}if(key==='btts')return 80;return 90}
function mergeMarket(a,b){if(!a)return b;if(!b)return a;if(typeof a!=='object'||typeof b!=='object')return b;return {...a,...b}}
function collectMarkets(books){const map=new Map();for(const book of books){for(const [rawKey,value] of Object.entries(book.odds||{})){if(!value||typeof value!=='object')continue;const key=canonicalMarket(rawKey);if(marketKind(key)==='UNKNOWN')continue;if(!map.has(key))map.set(key,new Map());const rows=map.get(key);rows.set(book.slug,mergeMarket(rows.get(book.slug),value))}}return[...map.entries()].sort((a,b)=>rank(a[0])-rank(b[0]))}
function stages(market){if(!market||typeof market!=='object')return[];const rows=[['OPEN',market.opening??market.open],['CLOSE',market.closing??market.close],['LIVE',market.inplay??market.in_play??market.live]];return rows.filter(([,v])=>v&&typeof v==='object')}
function priceText(line,price){const p=num(price);if(p===null)return'—';return `${line===null||line===undefined||line===''?'':`${fmt(line)} @ `}${fmt(p)}`}
function ahText(line,price,away=false){const p=num(price);if(p===null)return'—';const own=away?invertLine(line):line;return `${own===null||own===undefined||own===''?'':`${signed(own)} @ `}${fmt(p)}`}
function stageRow(kind,label,value){const live=label==='LIVE',rowClass=`fom-price-row${live?' live':''}`,stage=`<span class="fom-stage-label${live?' live':''}">${live?'<i></i> LIVE':label}</span>`;if(kind==='AH'){const line=value.line??value.hdp??value.handicap,home=ahText(line,value.home??value.home_odds??value.homeOdds),away=ahText(line,value.away??value.away_odds??value.awayOdds,true);if(home==='—'&&away==='—')return'';return `<div class="fom-price-row cols-ah">${stage}<strong class="fom-price">${esc(home)}</strong><strong class="fom-price">${esc(away)}</strong></div>`}if(kind==='1X2'){const h=fmt(value.home),d=fmt(value.draw),a=fmt(value.away);if(h==='—'&&d==='—'&&a==='—')return'';return `<div class="fom-price-row cols-1x2">${stage}<strong class="fom-price">${esc(h)}</strong><strong class="fom-price">${esc(d)}</strong><strong class="fom-price">${esc(a)}</strong></div>`}if(kind==='TOTAL'){const line=value.line??value.total,over=priceText(line,value.over??value.over_odds??value.overOdds),under=priceText(line,value.under??value.under_odds??value.underOdds);if(over==='—'&&under==='—')return'';return `<div class="fom-price-row cols-total">${stage}<strong class="fom-price">${esc(over)}</strong><strong class="fom-price">${esc(under)}</strong></div>`}if(kind==='BTTS'){const yes=fmt(value.yes),no=fmt(value.no);if(yes==='—'&&no==='—')return'';return `<div class="fom-price-row cols-total">${stage}<strong class="fom-price">${esc(yes)}</strong><strong class="fom-price">${esc(no)}</strong></div>`}return''}
function columnHead(kind,home,away){if(kind==='AH')return `<div class="fom-colhead cols-ah"><span>STAGE</span><b title="${esc(home)}">${esc(home)}</b><b title="${esc(away)}">${esc(away)}</b></div>`;if(kind==='1X2')return `<div class="fom-colhead cols-1x2"><span>STAGE</span><b title="${esc(home)}">${esc(home)}</b><b>DRAW</b><b title="${esc(away)}">${esc(away)}</b></div>`;if(kind==='BTTS')return '<div class="fom-colhead cols-total"><span>STAGE</span><b>YES</b><b>NO</b></div>';return '<div class="fom-colhead cols-total"><span>STAGE</span><b>OVER</b><b>UNDER</b></div>'}
function marketHtml(key,bookMarkets,card){const kind=marketKind(key),home=team(card,'home'),away=team(card,'away'),bookRows=[];for(const book of BOOKS){const market=bookMarkets.get(book.slug);if(!market)continue;const rows=stages(market).map(([label,value])=>stageRow(kind,label,value)).filter(Boolean);if(rows.length)bookRows.push(`<section class="fom-book" data-book="${esc(book.slug)}"><header><b>${esc(book.name)}</b><small>${esc(book.role)}</small></header>${rows.join('')}</section>`)}if(!bookRows.length)return'';const title=LABELS[key]||String(key).replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());return `<section class="fom-market"><header class="fom-market-head"><div><b>${esc(title)}</b><small>${kind==='AH'?'Signed handicap shown on each team side':kind==='1X2'?'Home · Draw · Away':'Line + decimal price'}</small></div><span>${bookRows.length} BOOKS</span></header>${columnHead(kind,home,away)}<div class="fom-books">${bookRows.join('')}</div></section>`}
function ensureStyle(){if(document.getElementById('nomad343-full-odds-bulk-style'))return;const s=document.createElement('style');s.id='nomad343-full-odds-bulk-style';s.textContent=`.fom-board{position:relative;overflow:hidden;margin-top:8px;border:1px solid #2a3a2e;background:#0b120d}.fom-head{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:10px 12px;border-bottom:1px solid #253229}.fom-head b{font-size:11px}.fom-head small{display:block;margin-top:3px;color:#7f8d84;font-size:8px}.fom-count{font-size:8px;color:#70dc91;font-weight:900;text-align:right}.fom-count.muted{color:#9b8d65}.fom-team-strip{display:grid;grid-template-columns:1fr 36px 1fr;gap:8px;align-items:center;padding:8px 12px;border-bottom:1px solid #202c24}.fom-team-strip .home{text-align:right}.fom-team-strip .away{text-align:left}.fom-team-strip small{display:block;color:#738078;font-size:7px}.fom-team-strip strong{display:block;font-size:10px}.fom-team-strip>i{text-align:center;color:#5f6b63;font-size:8px;font-style:normal}.fom-grid{display:grid;gap:8px;padding:8px}.fom-market{border:1px solid #27362c;background:#101813;min-width:0}.fom-market-head{display:flex;justify-content:space-between;align-items:flex-start;gap:8px;padding:8px 9px;border-bottom:1px solid #253229}.fom-market-head b{font-size:10px}.fom-market-head small{display:block;color:#748078;font-size:7px;margin-top:2px}.fom-market-head>span{font-size:7px;color:#6fdc91;font-weight:900}.fom-colhead,.fom-price-row{display:grid;gap:4px;align-items:center}.cols-ah,.cols-total{grid-template-columns:52px minmax(0,1fr) minmax(0,1fr)}.cols-1x2{grid-template-columns:52px repeat(3,minmax(0,1fr))}.fom-colhead{padding:5px 7px;background:#0b110d;color:#748078;font-size:7px}.fom-colhead b{text-align:center;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.fom-books{display:grid;gap:1px;background:#223027}.fom-book{background:#101813;padding:7px}.fom-book>header{display:flex;justify-content:space-between;gap:8px;margin-bottom:5px}.fom-book>header b{font-size:9px}.fom-book>header small{font-size:7px;color:#77847b}.fom-price-row{min-height:24px;border-top:1px solid rgba(255,255,255,.035)}.fom-stage-label{font-size:7px;color:#7b877f;font-weight:900}.fom-stage-label.live{color:#69df8d}.fom-stage-label.live i{display:inline-block;width:4px;height:4px;border-radius:50%;background:#69df8d;margin-right:3px}.fom-price{text-align:center;font-size:9px;font-variant-numeric:tabular-nums}.fom-price-row.live .fom-price{color:#69df8d;font-weight:900;text-shadow:0 0 8px rgba(105,223,141,.32)}.fom-board.fom-refresh-sweep::after{content:"";position:absolute;inset:0;z-index:6;pointer-events:none;background:linear-gradient(100deg,transparent 0 41%,rgba(80,255,140,0) 43%,rgba(80,255,140,.28) 49%,rgba(150,255,185,.18) 52%,rgba(80,255,140,0) 58%,transparent 60% 100%);transform:translateX(-120%);animation:fomRefreshSweep .9s ease-out 1}@keyframes fomRefreshSweep{0%{transform:translateX(-120%);opacity:0}12%{opacity:1}100%{transform:translateX(120%);opacity:0}}.fom-empty{padding:14px;text-align:center;color:#78857d;font-size:9px}@media(min-width:1000px){.fom-grid{grid-template-columns:1fr 1fr}}`;document.head.appendChild(s)}
function renderCard(card,fixture){if(!card||!fixture)return;ensureStyle();let host=card.querySelector('[data-full-odds-main]');if(!host){const details=card.querySelector('.event-details');if(!details)return;host=document.createElement('div');host.dataset.fullOddsMain='1';host.className='stable-market-slot';details.appendChild(host)}const books=providerBooks(fixture),markets=collectMarkets(books),rows=markets.map(([key,map])=>marketHtml(key,map,card)).filter(Boolean),home=team(card,'home'),away=team(card,'away'),stamp=fixture?.providerOddsUpdatedAt??fixture?.updatedAt??null;const meta=`${books.length}/10 BOOKS · BULK SNAPSHOT · 0 EXTRA REQUESTS`;if(!rows.length){host.innerHTML=`<section class="fom-board"><div class="fom-head"><div><b>FULL MARKET · BULK MODE</b><small>${esc(home)} vs ${esc(away)} · no click request</small></div><span class="fom-count muted">${esc(meta)}</span></div><div class="fom-team-strip"><div class="home"><small>HOME · LEFT</small><strong>${esc(home)}</strong></div><i>VS</i><div class="away"><small>AWAY · RIGHT</small><strong>${esc(away)}</strong></div></div><div class="fom-empty">Bulk snapshot ยังไม่มีราคาตลาดสำหรับคู่นี้ในรอบล่าสุด</div></section>`;return}host.innerHTML=`<section class="fom-board"><div class="fom-head"><div><b>FULL MARKET · BULK MODE</b><small>กดเปิดแล้วอ่านจาก snapshot ในหน่วยความจำทันที${stamp?` · source ${new Date(Number(stamp)).toLocaleTimeString()}`:''}</small></div><span class="fom-count">${esc(meta)} · ${rows.length} MARKETS</span></div><div class="fom-team-strip"><div class="home"><small>HOME · LEFT</small><strong>${esc(home)}</strong></div><i>VS</i><div class="away"><small>AWAY · RIGHT</small><strong>${esc(away)}</strong></div></div><div class="fom-grid">${rows.join('')}</div></section>`}
function hydrate(){document.querySelectorAll('.match-card[data-match-id][aria-expanded="true"]').forEach(card=>{const f=window.NOMAD343_LIVE?.getFixture?.(card.dataset.matchId);if(f)renderCard(card,f)})}
ensureStyle();window.NOMAD343_FULL_ODDS_BULK={version:VERSION,renderCard,hydrate,books:BOOKS.map(x=>({...x})),mode:'BULK_ONLY_ZERO_CLICK_REQUESTS'};queueMicrotask(hydrate);
})();
