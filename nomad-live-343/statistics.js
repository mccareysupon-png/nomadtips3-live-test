(()=>{'use strict';const API='/api/engine/statistics',POLL=45000;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));const show=v=>v===null||v===undefined?'—':String(v);const marketLabel=m=>({ah:'Asian Handicap',oneXtwo:'1X2',over:'OVER',under:'UNDER'}[m]||m);function dt(ms){try{return new Intl.DateTimeFormat('th-TH',{timeZone:'Asia/Bangkok',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(ms))}catch{return'—'}}function resultTag(r){const cls=r==='WIN'||r==='HALF_WIN'?'win':r==='LOSS'||r==='HALF_LOSS'?'loss':'push';return`<span class="tag ${cls}">${esc(r||'—')}</span>`}function set(q,v){const el=document.querySelector(q);if(el)el.textContent=show(v)}function render(j){set('[data-total]',j.total);set('[data-win]',j.win);set('[data-loss]',j.loss);set('[data-win-rate]',j.winRate===null?'—':`${j.winRate}%`);set('[data-market-ah]',j.byMarket?.ah);set('[data-market-oneXtwo]',j.byMarket?.oneXtwo);set('[data-market-over]',j.byMarket?.over);set('[data-market-under]',j.byMarket?.under);const tbody=document.querySelector('[data-stat-body]'),rows=Array.isArray(j.rows)?j.rows:[];if(tbody){tbody.innerHTML=rows.length?rows.map(s=>{const league=[s.league?.country,s.league?.name].filter(Boolean).join(' · '),scoreAt=`${show(s.scoreAt?.home)}-${show(s.scoreAt?.away)}`,final=`${show(s.finalScore?.home)}-${show(s.finalScore?.away)}`;return`<tr><td>${esc(dt(s.createdAt))}</td><td><span class="match-name">${esc(s.home?.name||'—')} - ${esc(s.away?.name||'—')}</span><span class="sub">${esc(league||'—')}</span></td><td>${esc(marketLabel(s.market))}</td><td>${esc(s.selection)}</td><td>${esc(show(s.line))}</td><td class="odds">${esc(show(s.odds))}</td><td>${esc(scoreAt)}</td><td>${esc(final)}</td><td>${resultTag(s.result)}</td></tr>`}).join(''):'<tr><td colspan="9" class="empty">ยังไม่มีสัญญาณที่จบการแข่งขันและตัดสินผลแล้ว</td></tr>'}const st=document.querySelector('[data-stat-state]');if(st)st.innerHTML=`<span class="pill"><i class="dot live"></i>Settlement · LIVE${j.halfWin||j.halfLoss?` · ครึ่งชนะ ${j.halfWin||0} / ครึ่งแพ้ ${j.halfLoss||0}`:''}</span>`}async function load(){try{const r=await fetch(`${API}?_=${Date.now()}`,{cache:'no-store'}),j=await r.json();if(!r.ok||j?.ok!==true)throw new Error();render(j)}catch{const st=document.querySelector('[data-stat-state]');if(st)st.innerHTML='<span class="pill"><i class="dot warn"></i>Settlement ไม่พร้อม</span>'}}load();setInterval(load,POLL)})();
