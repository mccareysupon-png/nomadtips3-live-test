(()=>{
'use strict';
const STAT_API='/api/engine/statistics';
const PAGE_SIZE=50;
const STAT_CACHE_MS=300000;
const $=s=>document.querySelector(s);
const $$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>v===null||v===undefined||v===''||typeof v==='boolean'||!Number.isFinite(Number(v))?null:Number(v);
const show=v=>v===null||v===undefined||v===''?'—':String(v);
const score=v=>{if(typeof v==='string'&&v.trim())return v;if(Array.isArray(v))return`${show(v[0])}-${show(v[1])}`;if(v&&typeof v==='object')return`${show(v.home??v.h)}-${show(v.away??v.a)}`;return'—'};
const dateMs=v=>{const n=num(v);if(n!==null)return n<1e12?n*1000:n;const p=Date.parse(String(v||''));return Number.isFinite(p)?p:0};
const when=v=>{const ms=dateMs(v);if(!ms)return'—';try{return new Intl.DateTimeFormat(navigator.language||'en-GB',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(ms))}catch{return'—'}};

const MARKETS=[
 {id:'all',label:'Total',title:'Total · All Markets',desc:'All settled Ball46 signals across every tracked market.'},
 {id:'1x2',label:'1X2',title:'1X2 · Match Result',desc:'Settled home, draw and away match-result signals.'},
 {id:'ah',label:'Asian Handicap',title:'AH · Asian Handicap',desc:'Settled Asian Handicap signals grouped by side and line.'},
 {id:'ou',label:'Over / Under',title:'Over / Under · Totals',desc:'Settled goal-total signals grouped by direction and line.'},
 {id:'btts',label:'BTTS',title:'BTTS · Both Teams To Score',desc:'Settled Yes / No both-teams-to-score signals.'},
 {id:'corners',label:'Corners',title:'Corners · All Corner Markets',desc:'Settled corner result, handicap and total markets.'},
 {id:'cards',label:'Cards',title:'Cards · All Card Markets',desc:'Settled card result, handicap and total markets.'},
 {id:'other',label:'Other',title:'Other · Remaining Markets',desc:'Every settled market outside the primary Ball46 families.'}
];
const state={view:'live',market:'all',variant:'all',filter:'all',page:1,rows:[],statsLoaded:false,statsLoading:false,statsLoadedAt:0};

function marketDef(r){return window.BALL46_MARKET_REGISTRY?.resolve?.(r)||null}
function marketKey(r){const d=marketDef(r);if(d?.family)return d.family;const x=String(r?.marketLabel||r?.market||r?.providerMarket||'').toLowerCase();if(/corner/.test(x))return'corners';if(/card/.test(x))return'cards';if(/btts|both teams/.test(x))return'btts';if(/asian|handicap|\bah\b/.test(x))return'ah';if(/over|under|o\/u|total/.test(x))return'ou';if(/1x2|match result|moneyline|winner|3way|three way/.test(x))return'1x2';return'other'}
function selectionKey(r){const x=String(r?.selection||r?.pick||'').trim().toUpperCase();if(x.includes('HOME'))return'home';if(x.includes('AWAY'))return'away';if(x.includes('DRAW')||x==='X')return'draw';if(x.includes('OVER'))return'over';if(x.includes('UNDER'))return'under';if(x==='YES'||x.includes('BTTS YES'))return'yes';if(x==='NO'||x.includes('BTTS NO'))return'no';return x.toLowerCase().replace(/[^a-z0-9]+/g,'_')||'other'}
function outcome(r){return String(r?.result||r?.settlement||r?.outcome||'').toUpperCase()}
function outcomeClass(x){return x==='WIN'||x==='HALF_WIN'?'win':x==='LOSS'||x==='HALF_LOSS'?'loss':'push'}
function outcomeWeight(x){if(x==='WIN')return 1;if(x==='HALF_WIN')return .5;if(x==='LOSS')return-1;if(x==='HALF_LOSS')return-.5;return 0}
function rowsForMarket(){let rows=state.market==='all'?state.rows:state.rows.filter(r=>marketKey(r)===state.market);if(state.variant!=='all')rows=rows.filter(r=>marketDef(r)?.key===state.variant);return rows}
function filteredRows(){let rows=rowsForMarket();if(state.filter!=='all')rows=rows.filter(r=>selectionKey(r)===state.filter);return rows.slice().sort((a,b)=>dateMs(b?.settledAt??b?.createdAt)-dateMs(a?.settledAt??a?.createdAt))}
function marketCounts(){const out=Object.fromEntries(MARKETS.map(m=>[m.id,0]));out.all=state.rows.length;state.rows.forEach(r=>{const k=marketKey(r);if(k in out)out[k]++});return out}
function filters(){if(state.market==='all')return[{id:'all',label:'All results'}];const rows=rowsForMarket(),labels={home:'Home',draw:'Draw',away:'Away',over:'Over',under:'Under',yes:'Yes',no:'No'},map=new Map([['all','All']]);rows.forEach(r=>{const k=selectionKey(r);if(!map.has(k))map.set(k,labels[k]||String(r?.selection||k).toUpperCase())});const order=['all','home','draw','away','over','under','yes','no'];return[...map.entries()].map(([id,label])=>({id,label,count:id==='all'?rows.length:rows.filter(r=>selectionKey(r)===id).length})).sort((a,b)=>{const ai=order.indexOf(a.id),bi=order.indexOf(b.id);return(ai<0?99:ai)-(bi<0?99:bi)})}
function currentMarket(){return MARKETS.find(m=>m.id===state.market)||MARKETS[0]}

function parseRoute(){const q=new URLSearchParams(location.search),view=q.get('view'),market=q.get('market'),variant=q.get('variant'),filter=q.get('filter');state.view=['live','signal','statistics'].includes(view)?view:'live';state.market=MARKETS.some(m=>m.id===market)?market:'all';const allowed=window.BALL46_MARKET_REGISTRY?.variants?.(state.market)||[];state.variant=allowed.some(v=>v.key===variant)?variant:'all';state.filter=filter||'all';state.page=Math.max(1,Number(q.get('page'))||1);return q.get('status')||'all'}
function writeRoute({replace=false,status=null}={}){const q=new URLSearchParams();if(state.view!=='live')q.set('view',state.view);if(state.view==='statistics'&&state.market!=='all')q.set('market',state.market);if(state.view==='statistics'&&state.variant!=='all')q.set('variant',state.variant);if(state.view==='statistics'&&state.filter!=='all')q.set('filter',state.filter);if(state.view==='statistics'&&state.page>1)q.set('page',state.page);if(state.view==='live'&&status&&status!=='all')q.set('status',status);const url=`${location.pathname}${q.toString()?`?${q}`:''}`;(replace?history.replaceState:history.pushState).call(history,null,'',url)}
function paintHeading(){const h=$('[data-workspace-heading]'),p=$('[data-workspace-subtitle]'),s=$('[data-workspace-kicker]');if(!h||!p||!s)return;if(state.view==='live'){s.textContent='NOMAD LIVE 3.43 · WORKSPACE';h.textContent="Today's Matches";p.textContent='Live, upcoming, waiting and finished fixtures in one compact workspace.'}else if(state.view==='signal'){s.textContent='NOMAD LIVE 3.43 · SIGNAL';h.textContent='Active Signals';p.textContent='Signal Center stays inside Page 1 and only reads the existing Signal Engine.'}else{s.textContent='NOMAD LIVE 3.43 · STATISTICS';h.textContent='Statistics';p.textContent='Performance, trend and settled history stay inside the same Page 1 workspace.'}}
function dispatchView(){document.body.dataset.workspaceView=state.view;document.dispatchEvent(new CustomEvent('ball46:workspace-view',{detail:{view:state.view,market:state.market}}))}
function stopStatistics(){}
function startStatistics(){if(state.statsLoaded&&Date.now()-state.statsLoadedAt<STAT_CACHE_MS){renderStats();return}loadStatistics()}
function setView(view,{market=null,push=true}={}){state.view=view;if(market){state.market=market;state.variant='all';state.filter='all';state.page=1}$$('[data-workspace-panel]').forEach(p=>p.hidden=p.dataset.workspacePanel!==view);$$('[data-workspace-view]').forEach(b=>b.classList.toggle('active',b.dataset.workspaceView===view));$$('[data-stat-market]').forEach(b=>b.classList.toggle('active',view==='statistics'&&b.dataset.statMarket===state.market));const leagues=$('[data-league-card]');if(leagues)leagues.hidden=false;paintHeading();if(view==='statistics'){startStatistics();if(state.statsLoaded)renderStats()}else stopStatistics();dispatchView();if(push)writeRoute()}
function bindNavigation(){
 $$('[data-workspace-view]').forEach(b=>b.addEventListener('click',()=>setView(b.dataset.workspaceView)));
 $$('[data-stat-market]').forEach(b=>b.addEventListener('click',()=>setView('statistics',{market:b.dataset.statMarket})));
 $$('[data-status-filter]').forEach(b=>b.addEventListener('click',()=>{setView('live',{push:false});writeRoute({status:b.dataset.statusFilter||'all'})}));
 window.addEventListener('popstate',()=>{const status=parseRoute();setView(state.view,{push:false});if(state.view==='live'&&status!=='all')setTimeout(()=>document.querySelector(`[data-status-filter="${CSS.escape(status)}"]`)?.click(),0)});
}
function mirrorSignalCount(){const source=$('[data-signal-count]'),target=$('[data-workspace-signal-count]');if(!source||!target)return;const sync=()=>target.textContent=source.textContent||'0';sync();new MutationObserver(sync).observe(source,{childList:true,subtree:true,characterData:true})}

function renderStatNav(){const c=marketCounts();$$('[data-stat-market]').forEach(b=>{const id=b.dataset.statMarket;b.classList.toggle('active',state.view==='statistics'&&state.market===id);const count=b.querySelector('b');if(count)count.textContent=String(c[id]||0)})}
function renderStatHeader(){const m=currentMarket(),f=filters();if(!f.some(x=>x.id===state.filter))state.filter='all';$('[data-sp-market-title]').textContent=m.title;$('[data-sp-market-desc]').textContent=m.desc;const selected=f.find(x=>x.id===state.filter),variant=state.variant==='all'?null:window.BALL46_MARKET_REGISTRY?.definitions?.[state.variant];$('[data-sp-filter-label]').textContent=[variant?.short,selected?.label||'All results'].filter(Boolean).join(' · ')}
function renderKpis(rows){let win=0,loss=0,push=0;rows.forEach(r=>{const c=outcomeClass(outcome(r));if(c==='win')win++;else if(c==='loss')loss++;else push++});const rate=(win+loss)?(win/(win+loss)*100):null,odds=rows.map(r=>num(r?.odds)).filter(v=>v!==null&&v>1),avg=odds.length?odds.reduce((a,b)=>a+b,0)/odds.length:null;const vals={total:rows.length,win,loss,push,rate:rate===null?'—':`${rate.toFixed(1)}%`,odds:avg===null?'—':avg.toFixed(2)};Object.entries(vals).forEach(([k,v])=>{const el=$(`[data-sp-kpi="${k}"]`);if(el)el.textContent=v})}
function renderGraph(rows){const path=$('[data-sp-spark-line]'),note=$('[data-sp-range]');if(!path)return;const data=rows.slice().sort((a,b)=>dateMs(a?.settledAt??a?.createdAt)-dateMs(b?.settledAt??b?.createdAt)).slice(-30);if(note)note.textContent=data.length?`Last ${data.length} settled signals`:'No settled signals';if(!data.length){path.setAttribute('d','M0 60 L1000 60');return}let total=0;const pts=data.map((r,i)=>({x:i,y:(total+=outcomeWeight(outcome(r)))})),ys=pts.map(p=>p.y),min=Math.min(0,...ys),max=Math.max(0,...ys),span=Math.max(1,max-min);path.setAttribute('d',pts.map((p,i)=>{const x=pts.length===1?500:(p.x/(pts.length-1))*1000,y=108-((p.y-min)/span)*96;return`${i?'L':'M'}${x.toFixed(1)} ${y.toFixed(1)}`}).join(' '))}
function renderVariants(){const box=$('[data-sp-variant-filters]'),wrap=$('[data-sp-variant-chips]');if(!box||!wrap)return;const variants=state.market==='all'?[]:(window.BALL46_MARKET_REGISTRY?.variants?.(state.market)||[]);box.hidden=!variants.length;if(!variants.length){state.variant='all';wrap.innerHTML='';return}if(state.variant!=='all'&&!variants.some(v=>v.key===state.variant))state.variant='all';const list=[{key:'all',short:'All types'},...variants];wrap.innerHTML=list.map(v=>`<button type="button" class="sp-filter-chip ${state.variant===v.key?'active':''}" data-sp-variant="${esc(v.key)}">${esc(v.short||v.label||v.key)}</button>`).join('');$$('[data-sp-variant]').forEach(b=>b.addEventListener('click',()=>{state.variant=b.dataset.spVariant;state.filter='all';state.page=1;writeRoute();renderStats()}))}
function renderFilters(){const f=filters(),wrap=$('[data-sp-filter-chips]');if(!wrap)return;wrap.innerHTML=f.map(x=>`<button type="button" class="sp-filter-chip ${state.filter===x.id?'active':''}" data-sp-filter="${esc(x.id)}">${esc(x.label)}${x.count===undefined?'':` · ${x.count}`}</button>`).join('');$$('[data-sp-filter]').forEach(b=>b.addEventListener('click',()=>{state.filter=b.dataset.spFilter;state.page=1;writeRoute();renderStats()}))}
function selectionLabel(r){const x=String(r?.selection||r?.pick||'').toUpperCase();if(x==='HOME')return r?.home?.name||'HOME';if(x==='AWAY')return r?.away?.name||'AWAY';if(x==='DRAW')return'DRAW';return show(r?.selection||r?.pick).toUpperCase()}
function lineLabel(r){const n=num(r?.line);if(n===null)return'—';const t=Number.isInteger(n)?String(n):String(Math.round(n*100)/100);return marketKey(r)==='ah'&&n>0?`+${t}`:t}
function renderTable(rows){const total=rows.length,pages=Math.max(1,Math.ceil(total/PAGE_SIZE));if(state.page>pages)state.page=pages;const slice=rows.slice((state.page-1)*PAGE_SIZE,state.page*PAGE_SIZE),body=$('[data-sp-table-body]');$('[data-sp-record-count]').textContent=`${total} record${total===1?'':'s'}`;body.innerHTML=slice.length?slice.map(r=>{const league=[r?.league?.country,r?.league?.name].filter(Boolean).join(' · '),res=outcome(r)||'PUSH';return`<tr><td>${esc(when(r?.settledAt??r?.createdAt))}</td><td><div class="sp-match"><strong>${esc(r?.home?.name||'—')} — ${esc(r?.away?.name||'—')}</strong><small>${esc(league||'—')}</small></div></td><td>${esc(r?.marketLabel||r?.market||r?.providerMarket||'—')}</td><td>${esc(selectionLabel(r))}</td><td>${esc(lineLabel(r))}</td><td>${esc(show(r?.odds))}</td><td>${esc(score(r?.entryScore??r?.scoreAt))}</td><td>${esc(score(r?.finalScore))}</td><td><span class="sp-result ${outcomeClass(res)}">${esc(res)}</span></td></tr>`}).join(''):'<tr><td colspan="9"><div class="sp-empty">No settled results in this view.</div></td></tr>';renderPager(pages)}
function renderPager(pages){const el=$('[data-sp-pager]');if(!el)return;if(pages<=1){el.innerHTML='';return}const keep=[];for(let p=1;p<=pages;p++)if(p===1||p===pages||Math.abs(p-state.page)<=1)keep.push(p);let last=0,html=[`<button class="sp-page" data-sp-page="${Math.max(1,state.page-1)}" ${state.page===1?'disabled':''}>‹</button>`];keep.forEach(p=>{if(last&&p-last>1)html.push('<span>…</span>');html.push(`<button class="sp-page ${p===state.page?'active':''}" data-sp-page="${p}">${p}</button>`);last=p});html.push(`<button class="sp-page" data-sp-page="${Math.min(pages,state.page+1)}" ${state.page===pages?'disabled':''}>›</button>`);el.innerHTML=html.join('');$$('[data-sp-page]').forEach(b=>b.addEventListener('click',()=>{state.page=Number(b.dataset.spPage)||1;writeRoute();renderStats()}))}
function renderStats(){if(!state.statsLoaded)return;renderStatNav();renderVariants();renderStatHeader();const rows=filteredRows();renderKpis(rows);renderGraph(rows);renderFilters();renderTable(rows)}
async function loadStatistics(){if(state.view!=='statistics'||state.statsLoading)return;state.statsLoading=true;try{const r=await fetch(`${STAT_API}?_=${Date.now()}`,{cache:'no-store'}),j=await r.json();if(!r.ok||j?.ok!==true||!Array.isArray(j?.rows))throw new Error('statistics unavailable');state.rows=j.rows;state.statsLoaded=true;state.statsLoadedAt=Date.now();renderStats();const st=$('[data-sp-feed-state]');if(st)st.textContent=`Updated · ${state.rows.length} settled`;}catch(err){console.warn('Single-page statistics load failed',err);const st=$('[data-sp-feed-state]');if(st)st.textContent='Statistics feed unavailable'}finally{state.statsLoading=false}}

function init(){const status=parseRoute();bindNavigation();mirrorSignalCount();setView(state.view,{push:false});if(state.view==='live'&&status!=='all')setTimeout(()=>document.querySelector(`[data-status-filter="${CSS.escape(status)}"]`)?.click(),50)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
window.addEventListener('beforeunload',stopStatistics,{once:true});
})();
