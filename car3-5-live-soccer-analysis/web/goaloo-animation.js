(()=>{
const $=s=>document.querySelector(s),clamp=v=>Math.max(0,Math.min(1,Number(v)||0)),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
let current=null,timer=null,pollMs=1500,seen=new Set(),seenPoints=new Map(),queue=[],playing=false,ball={x:.5,y:.5},roll=0,generation=0,payload=null;
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
function matchId(){const el=$('.signal-item.active[data-id]')||$('.signal-item[data-id]'),id=String(el?.dataset.id||'');return /^\d+$/.test(id)?id:null}
function ui(){const pitch=$('#pitch');if(!pitch)return null;if(!$('#sourceBall')){const style=document.createElement('style');style.textContent=`#sourceBall{position:absolute;z-index:7;left:0;top:0;width:17px;height:17px;border:2px solid #fff;background:radial-gradient(circle at 35% 30%,#fff 0 16%,#dfe3e6 17% 31%,#11151a 32% 44%,#f7f7f7 45% 68%,#15191e 69% 100%);border-radius:50%;box-shadow:0 3px 12px #000b,0 0 7px #fff,0 0 18px rgba(243,198,35,.78);opacity:0;pointer-events:none;will-change:transform}#sourceTrail{position:absolute;z-index:6;height:4px;border-radius:999px;background:linear-gradient(90deg,rgba(243,198,35,0),rgba(243,198,35,.2),rgba(255,255,255,.9));transform-origin:right center;opacity:0;pointer-events:none}#pitch.source-ball-active #sourceBall{opacity:1}#pitch.source-ball-active #sourceTrail{opacity:.72}#pitch.source-ball-active #ball,#pitch.source-ball-active #zoneGlow{opacity:0!important}`;document.head.appendChild(style);pitch.insertAdjacentHTML('beforeend','<div id="sourceTrail"></div><div id="sourceBall"></div>')}return pitch}
function active(on){ui()?.classList.toggle('source-ball-active',Boolean(on))}
function setBall(x,y,angle=roll,from=ball){const pitch=ui(),el=$('#sourceBall'),trail=$('#sourceTrail');if(!pitch||!el)return;const r=pitch.getBoundingClientRect(),px=clamp(x)*r.width,py=clamp(y)*r.height;el.style.transform=`translate3d(${px}px,${py}px,0) translate(-50%,-50%) rotate(${angle}deg)`;if(trail){const fx=clamp(from.x)*r.width,fy=clamp(from.y)*r.height,dx=px-fx,dy=py-fy,dist=Math.hypot(dx,dy);trail.style.left=`${px}px`;trail.style.top=`${py}px`;trail.style.width=`${Math.max(12,Math.min(105,dist))}px`;trail.style.transform=`translate(-100%,-50%) rotate(${Math.atan2(dy,dx)*180/Math.PI}deg)`;trail.style.opacity=dist>2?'.72':'0'}}
function move(to,duration=360,token=generation){return new Promise(resolve=>{const start={...ball},end={x:clamp(to.x),y:clamp(to.y)},pitch=ui(),r=pitch?.getBoundingClientRect(),dist=r?Math.hypot((end.x-start.x)*r.width,(end.y-start.y)*r.height):0,startRoll=roll,endRoll=roll+dist*7.6;if(reduced){ball=end;roll=endRoll;setBall(ball.x,ball.y,roll,start);return resolve()}const begun=performance.now();function tick(now){if(token!==generation)return resolve();const p=Math.min(1,(now-begun)/duration),e=1-Math.pow(1-p,3);ball={x:start.x+(end.x-start.x)*e,y:start.y+(end.y-start.y)*e};setBall(ball.x,ball.y,startRoll+(endRoll-startRoll)*e,start);if(p<1)requestAnimationFrame(tick);else{roll=endRoll;resolve()}}requestAnimationFrame(tick)})}
function hasXY(e){return e?.coordinateSource==='SOURCE_XY'&&((e.points||[]).some(p=>Number.isFinite(Number(p.x))&&Number.isFinite(Number(p.y)))||(Number.isFinite(Number(e?.x))&&Number.isFinite(Number(e?.y))))}
function team(e){return e?.team==='HOME'?payload?.match?.home:e?.team==='AWAY'?payload?.match?.away:'Unknown team'}
function label(e){const min=Number.isFinite(Number(e?.minute))?`${Number(e.minute)}'`:'LIVE',inj=Number(e?.injuryMinute)>0?`+${Number(e.injuryMinute)}`:'',player=String(e?.playerName||'').trim();return `${min}${inj} · ${team(e)} · ${String(e?.type||'LIVE ACTIVITY')}${player?` · ${player}`:''}`}
function show(e){const pe=$('#pitchEvent');if(pe)pe.textContent=label(e)}
function events(){const list=$('#eventList'),count=$('#eventCount'),arr=[...(payload?.events||[])].sort((a,b)=>(Number(b.id)||0)-(Number(a.id)||0)).slice(0,20);if(count)count.textContent=`${arr.length} events`;if(list)list.innerHTML=arr.length?arr.map(e=>`<div class="event"><time>${esc(Number.isFinite(Number(e.minute))?e.minute:'—')}'</time><span>${esc(e.type||'Event')}</span><small>${esc(team(e))}${e.playerName?` · ${esc(e.playerName)}`:''}</small></div>`).join(''):'<div class="event-empty">Waiting for live event data</div>'}
function fallbackPoint(e){const side=String(e?.team||'').toUpperCase(),home=side==='HOME',away=side==='AWAY';if(!home&&!away)return null;const type=String(e?.type||'').toLowerCase();let x=home?.66:.34,y=.5;if(type.includes('goal kick'))x=home?.12:.88;else if(type.includes('corner')){x=home?.96:.04;y=/left/i.test(type)?.08:/right/i.test(type)?.92:.5}else if(type.includes('goal'))x=home?.97:.03;else if(type.includes('shot'))x=home?.90:.10;else if(type.includes('danger'))x=home?.84:.16;else if(type.includes('attack'))x=home?.73:.27;else if(type.includes('free kick'))x=home?.68:.32;else if(type.includes('foul'))x=home?.58:.42;else return null;return{x,y}}
async function animate(e,token){show(e);active(true);if(hasXY(e)){const pts=(e.points||[]).filter(p=>Number.isFinite(Number(p.x))&&Number.isFinite(Number(p.y))).map(p=>({x:p.x,y:p.y}));if(!pts.length)pts.push({x:e.x,y:e.y});const each=Math.max(130,Math.min(420,1000/pts.length));for(const p of pts){if(token!==generation)return;await move(p,each,token)}return}const target=fallbackPoint(e);if(target)await move(target,520,token)}
async function drain(){if(playing)return;playing=true;while(queue.length){const item=queue.shift();if(item.id!==current||item.token!==generation)continue;await animate(item.event,item.token)}playing=false}
function reset(id){generation++;current=id;seen=new Set();seenPoints=new Map();queue=[];playing=false;ball={x:.5,y:.5};roll=0;active(Boolean(id));setBall(.5,.5,0,ball)}
function ingest(p,id){payload=p;pollMs=Math.max(1000,Math.min(5000,Number(p.pollMs)||1500));events();active(true);const incoming=p.events||[];if(!seen.size)for(const e of incoming.slice(0,-1)){const k=String(e.id);seen.add(k);const ids=(e.points||[]).map(x=>Number(x.id)).filter(Number.isFinite);if(ids.length)seenPoints.set(k,Math.max(...ids))}for(const e of incoming.filter(e=>!seen.has(String(e.id))).slice(-8)){const k=String(e.id);seen.add(k);const ids=(e.points||[]).map(x=>Number(x.id)).filter(Number.isFinite);if(ids.length)seenPoints.set(k,Math.max(...ids));queue.push({id,event:e,token:generation})}const e=p.current;if(e&&seen.has(String(e.id))&&hasXY(e)){const k=String(e.id),last=seenPoints.get(k)??-Infinity,newPts=(e.points||[]).filter(x=>Number.isFinite(Number(x.id))&&Number(x.id)>last);if(newPts.length){seenPoints.set(k,Math.max(...newPts.map(x=>Number(x.id))));const z=newPts.at(-1);queue.push({id,event:{...e,points:newPts,x:z.x,y:z.y},token:generation})}else show(e)}else if(e)show(e);drain()}
async function poll(){clearTimeout(timer);const id=matchId();if(!id){if(current)reset(null);timer=setTimeout(poll,1000);return}if(current!==id)reset(id);try{const r=await fetch(`/animation?id=${encodeURIComponent(id)}&t=${Date.now()}`,{cache:'no-store'}),p=await r.json();if(r.ok&&p?.ok&&String(p.matchId)===id)ingest(p,id)}catch(e){console.warn('Live animation unavailable',e)}timer=setTimeout(poll,pollMs)}
window.addEventListener('resize',()=>setBall(ball.x,ball.y,roll,ball));document.addEventListener('visibilitychange',()=>{if(!document.hidden)poll()});ui();setTimeout(poll,250);
})();
