name: Resolve NOMADTIPS3 Live Test Request — MACHINE 2 PARKED

# MACHINE 2 / CAR_2_TEST_ONLY is intentionally parked.
# No push trigger, no schedule, no API-Football calls.
# Manual dispatch is retained only as a visible placeholder and always skips.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Machine 2 parked
        run: echo "CAR_2_TEST_ONLY is parked; no engine or API work is allowed."
