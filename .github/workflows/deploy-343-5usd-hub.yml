name: Deploy 3.43 5USD Hub

on:
  push:
    branches:
      - work/nomad343-clean-bet365-ui
      - work/nomad343-dashboard-v2-bulk-20260915
    paths:
      - 'workers/nomadtips3-5usd-hub-343/**'
      - '.github/workflows/deploy-343-5usd-hub.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  FIVEDOLLAR_API_KEY: ${{ secrets.FIVEDOLLAR_API_KEY }}
  HUB_URL: https://nomadtips3-5usd-hub-343.mccarey-supon.workers.dev

jobs:
  deploy-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Verify current 3.43 hub scope
        run: |
          set -euo pipefail
          node --check workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq "const VERSION='nomad343-5usd-hub-v6-safe-control'" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq 'main = "src/index-v2.js"' workers/nomadtips3-5usd-hub-343/wrangler.toml
          grep -Fq "crons = [\"*/2 * * * *\"]" workers/nomadtips3-5usd-hub-343/wrangler.toml
          grep -Fq "x-ratelimit-remaining" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq "providerUsedInWindow" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq "OWNER_TOKEN_SHA256" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq "UNSAFE_PROJECTED_LOAD" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          grep -Fq "setAlarm" workers/nomadtips3-5usd-hub-343/src/index-v2.js
          if grep -R "nomadtips3-shot-sidecar-342\|nomadtips3-342-signal-engine" workers/nomadtips3-5usd-hub-343; then
            echo '::error::3.43 hub must not depend on 3.42 runtime'; exit 1
          fi

      - name: Dry-run hub bundle
        working-directory: workers/nomadtips3-5usd-hub-343
        run: npx --yes wrangler@4.92.0 deploy --dry-run --config wrangler.toml

      - name: Validate credentials
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_API_TOKEN" || { echo '::error::Missing CLOUDFLARE_API_TOKEN'; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo '::error::Missing CLOUDFLARE_ACCOUNT_ID'; exit 1; }
          test -n "$FIVEDOLLAR_API_KEY" || { echo '::error::Missing FIVEDOLLAR_API_KEY'; exit 1; }

      - name: Deploy 3.43 hub
        working-directory: workers/nomadtips3-5usd-hub-343
        run: npx --yes wrangler@4.92.0 deploy --config wrangler.toml

      - name: Bind 5USD API key
        working-directory: workers/nomadtips3-5usd-hub-343
        run: printf '%s' "$FIVEDOLLAR_API_KEY" | npx --yes wrangler@4.92.0 secret put FIVEDOLLAR_API_KEY --config wrangler.toml

      - name: Verify hub telemetry and guarded control without direct provider probe
        run: |
          set -euo pipefail
          curl -fsS --max-time 45 "$HUB_URL/snapshot?_=$RANDOM$RANDOM" -o /tmp/hub-snapshot.json
          curl -fsS --max-time 20 "$HUB_URL/health?_=$RANDOM$RANDOM" -o /tmp/hub-health.json
          curl -fsS --max-time 20 "$HUB_URL/control?_=$RANDOM$RANDOM" -o /tmp/hub-control.json
          unauth=$(curl -sS --max-time 20 -o /tmp/control-unauth.json -w '%{http_code}' -X POST -H 'content-type: application/json' --data '{"enabled":true,"intervalSec":120}' "$HUB_URL/control")
          test "$unauth" = "401"
          node - <<'NODE'
          const fs=require('fs');
          const s=JSON.parse(fs.readFileSync('/tmp/hub-snapshot.json','utf8'));
          const h=JSON.parse(fs.readFileSync('/tmp/hub-health.json','utf8'));
          const c=JSON.parse(fs.readFileSync('/tmp/hub-control.json','utf8'));
          const sample=(Array.isArray(s.fixtures)?s.fixtures:[]).slice(0,3).map(x=>({fixtureId:x.fixtureId,league:x.league?.name,home:x.home?.name,away:x.away?.name,minute:x.minute}));
          console.log(JSON.stringify({health:h,control:c,snapshot:{ok:s.ok,version:s.version,provider:s.provider,fixtureCount:s.fixtureCount,ageMs:s.ageMs,stale:s.stale,providerRequestCount:s.providerRequestCount,providerUsedInWindow:s.providerUsedInWindow,estimatedKnownRequestsPerMinute:s.estimatedKnownRequestsPerMinute,lastError:s.lastError},sample},null,2));
          if(s.ok!==true||h.ok!==true||c.ok!==true)process.exit(1);
          if(s.version!=='nomad343-5usd-hub-v6-safe-control'||h.version!=='nomad343-5usd-hub-v6-safe-control'||c.version!=='nomad343-5usd-hub-v6-safe-control')process.exit(2);
          if(!Array.isArray(c.allowedIntervalsSec)||!c.allowedIntervalsSec.includes(15)||!c.allowedIntervalsSec.includes(3))process.exit(3);
          if(!Object.prototype.hasOwnProperty.call(h,'providerHeadersAvailable'))process.exit(4);
          if(!Object.prototype.hasOwnProperty.call(h,'providerUsedInWindow'))process.exit(5);
          NODE
