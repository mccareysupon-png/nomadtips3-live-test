name: Nowgoal Source Probe

on:
  pull_request:
    paths:
      - 'tools/nowgoal-roster-sign-probe.mjs'
      - '.github/workflows/nowgoal-source-probe.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Inspect Nowgoal roster and match metadata
        run: node tools/nowgoal-roster-sign-probe.mjs
