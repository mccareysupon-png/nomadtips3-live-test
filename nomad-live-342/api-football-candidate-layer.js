(()=>{
'use strict';
const runtime=window.NOMAD342_MARKET_RUNTIME||{};
const ledgerRuntime=window.NOMAD342_LEDGER_RUNTIME||{};
const SETTINGS_ACTIVE_KEY='nomad342MarketSettingsActiveV3';
const SETTINGS_RUN_KEY='nomad342MarketRunV3';
function localJson(key){try{return JSON.parse(localStorage.getItem(key)||'{}')||{}}catch{return {}}}
const settingsApi=window.NOMAD342_MARKET_SETTINGS||Object.freeze({version:'market-settings-v3',activeKey:SETTINGS_ACTIVE_KEY,runKey:SETTINGS_RUN_KEY,runningSnapshot(){const settings=localJson(SETTINGS_ACTIVE_KEY),raw=localJson(SETTINGS_RUN_KEY);return {version:'market-settings-v3',settings,run:{over:Boolean(raw.over),under:Boolean(raw.under),oneXtwo:Boolean(raw.oneXtwo),updatedAt:finite(raw.updatedAt)}}}});
const STORE_KEY='nomad342ApiFootballPredictionsV2';
const LEDGER_OUTBOX_KEY='nomad342LedgerOutboxV2';
const STAT_HISTORY_KEY='nomad342ApiFootballStatsHistoryV1';
const FOOTBALL_HISTORY_KEY='nomad342FootballHistoryV1';
const MAX_ATTEMPTS=3;
const RETRY_DELAYS=[0,45000,120000];
const LEDGER_RETRY_DELAYS=[0,15000,45000,120000,300000];
const GATE_RECHECK_MS=15000;
const state=new Map();
let busy=false,ledgerBusy=false;
function finite(v){if(v===null||v===undefined||v===''||typeof v==='boolean')return null;const n=Number(v);return Number.isFinite(n)?n:null}
function clamp(v,min,max){return Math.max(min,Math.min(max,v))}
function esc(v){return String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
function loadJson(storage,key){try{return JSON.parse(storage.getItem(key)||'{}')||{}}catch{return {}}}
function saveJson(storage,key,value){try{storage.setItem(key,JSON.stringify(value));return true}catch{return false}}
function load(){return loadJson(sessionStorage,STORE_KEY)}
function save(store){saveJson(sessionStorage,STORE_KEY,store)}
function loadOutbox(){return loadJson(localStorage,LEDGER_OUTBOX_KEY)}
function saveOutbox(store){return saveJson(localStorage,LEDGER_OUTBOX_KEY,store)}
function loadStatHistory(){return loadJson(sessionStorage,STAT_HISTORY_KEY)}
function saveStatHistory(store){saveJson(sessionStorage,STAT_HISTORY_KEY,store)}
function lockedFor(id){const row=load()[String(id)];return row?.status==='PREDICTED'?row:null}
function remember(id,row){const store=load();store[String(id)]={...row,status:'PREDICTED',ledgerStatus:row?.ledgerStatus||'PENDING',savedAt:Date.now()};save(store)}
function setLedgerStatus(id,status,extra={}){const store=load(),key=String(id),row=store[key];if(!row)return;store[key]={...row,ledgerStatus:status,...extra};save(store)}
function runningSnapshot(){if(settingsApi&&typeof settingsApi.runningSnapshot==='function')return settingsApi.runningSnapshot();return {version:'market-settings-v3',settings:{},run:{over:false,under:false,oneXtwo:false}}}
function implied(values){const raw=values.map(v=>1/Math.max(1.001,Number(v)||999)),sum=raw.reduce((a,b)=>a+b,0);return raw.map(v=>sum>0?v/sum:0)}
function normalize3(a,b,c){const sum=a+b+c||1,raw=[a/sum*100,b/sum*100,c/sum*100],rounded=raw.map(Math.round);let diff=100-rounded.reduce((x,y)=>x+y,0);for(let i=0;diff!==0;i=(i+1)%3){rounded[i]+=diff>0?1:-1;diff+=diff>0?-1:1}return rounded}
function normalize2(a,b){const sum=a+b||1,aa=Math.round(a/sum*100);return [aa,100-aa]}
function prediction(r,market){const one=market.oneXtwo,tot=market.totals,em=r?.event?.metrics||{},m=r?.m||{};const [mh,md,ma]=implied([one.home,one.draw,one.away]);const pressure=clamp((finite(em.pressureShare)??50)/100,.35,.80),trend=clamp((finite(em.trendPass)??0)/3,0,1);const scoreH=finite(m?.score?.[0])??0,scoreA=finite(m?.score?.[1])??0;const homeBoost=clamp((pressure-.5)*.45+trend*.035+(scoreH-scoreA)*.025,0,.18),drawBoost=scoreH===scoreA?.018:0;const [homePct,drawPct,awayPct]=normalize3(mh+homeBoost,md+drawBoost,ma);const onePick=homePct>=awayPct?'HOME':'AWAY';const [mo,mu]=implied([tot.over,tot.under]);const hSot=finite(em.hSot)??0,aSot=finite(em.aSot)??0,hOff=finite(em.hOff)??0,aOff=finite(em.aOff)??0,hD=finite(em.hD)??0,aD=finite(em.aD)??0,hC=finite(em.hCorner)??0,aC=finite(em.aCorner)??0;const activity=clamp((hSot+aSot+hOff+aOff)*.018+(hD+aD)*.005+(hC+aC)*.01,0,.16),minute=finite(m.minute)??60,currentGoals=scoreH+scoreA,line=finite(tot.line)??2.5;let over=mo,under=mu;if(currentGoals>line){over=.99;under=.01}else{const remaining=clamp((95-minute)/40,0,1),near=currentGoals>=line-.5?.055:0;over=clamp(mo+activity*remaining+near,.05,.95);under=clamp(mu-activity*remaining-near,.05,.95)}const [overPct,underPct]=normalize2(over,under),ouPick=overPct>=underPct?'OVER':'UNDER';return {oneXtwo:{pick:onePick,home:homePct,draw:drawPct,away:awayPct},totals:{pick:ouPick,line,over:overPct,under:underPct}}}
function at(pair,index){return Array.isArray(pair)?finite(pair[index]):null}
function delta(first,last,key,index){const a=at(first?.[key],index),b=at(last?.[key],index);return a===null||b===null?null:b-a}
function share(a,b){a=finite(a);b=finite(b);if(a===null||b===null)return [null,null];a=Math.max(0,a);b=Math.max(0,b);const total=a+b;if(total<=0)return [null,null];const h=Number((a/total*100).toFixed(1));return [h,Number((100-h).toFixed(1))]}
function footballSnapshots(m){const now=Date.now(),store=loadJson(sessionStorage,FOOTBALL_HISTORY_KEY),key=String(m?.id||''),incoming=[...(m?.event?.snapshots||[])].filter(s=>finite(s?.minute)!==null);let rows=Array.isArray(store[key])?store[key]:[];const byMinute=new Map();for(const row of [...rows,...incoming]){const minute=finite(row?.minute);if(minute===null)continue;const prior=byMinute.get(minute);if(!prior||Number(row?.observedAt||0)>=Number(prior?.observedAt||0))byMinute.set(minute,row)}rows=[...byMinute.values()].filter(x=>{const t=finite(x?.observedAt);return t===null||now-t<=32*60*1000}).sort((a,b)=>Number(a.minute)-Number(b.minute)||Number(a.observedAt||0)-Number(b.observedAt||0)).slice(-40);store[key]=rows;saveJson(sessionStorage,FOOTBALL_HISTORY_KEY,store);return rows}
function rollingFootball(m,minutes){const current=finite(m?.minute),windowMinutes=Number(minutes),snaps=footballSnapshots(m);if(current===null||!Number.isFinite(windowMinutes))return null;const eligible=snaps.filter(s=>Number(s.minute)>=current-windowMinutes&&Number(s.minute)<=current),first=eligible[0],last=eligible[eligible.length-1];if(!first||!last||first===last||Number(first.minute)>=Number(last.minute))return null;const hA=delta(first,last,'attacks',0),aA=delta(first,last,'attacks',1),hD=delta(first,last,'dangerous',0),aD=delta(first,last,'dangerous',1),hCorner=delta(first,last,'corner',0),aCorner=delta(first,last,'corner',1);if([hA,aA,hD,aD,hCorner,aCorner].some(v=>v===null))return null;const [hAttackPct,aAttackPct]=share(hA,aA),[hDangerousPct,aDangerousPct]=share(hD,aD);return {from:Number(first.minute),to:Number(last.minute),hA,aA,hD,aD,hCorner,aCorner,hAttackPct,aAttackPct,hDangerousPct,aDangerousPct}}
function normalizeCandidateStats(stats){const h=stats?.home||{},a=stats?.away||{};return {home:{shotOnTarget:finite(h.shotOnTarget),shotOff:finite(h.shotOff),possessionPct:finite(h.possession)},away:{shotOnTarget:finite(a.shotOnTarget),shotOff:finite(a.shotOff),possessionPct:finite(a.possession)}}}
function rememberCandidateStats(id,market){const now=finite(market?.observedAt)??Date.now(),minute=finite(market?.fixture?.minute),stats=normalizeCandidateStats(market?.statistics),store=loadStatHistory(),key=String(id),rows=Array.isArray(store[key])?store[key]:[],row={observedAt:now,minute,stats};let next=rows.filter(x=>finite(x?.observedAt)!==null&&now-Number(x.observedAt)<=32*60*1000);if(minute!==null&&next.length&&finite(next[next.length-1]?.minute)===minute)next=[...next.slice(0,-1),row];else next.push(row);store[key]=next.slice(-40);saveStatHistory(store);return stats}
function candidateRollingStats(id,market,minutes){const latest=rememberCandidateStats(id,market),now=finite(market?.observedAt)??Date.now(),windowMs=Number(minutes)*60*1000,store=loadStatHistory(),rows=(Array.isArray(store[String(id)])?store[String(id)]:[]).filter(x=>now-Number(x.observedAt||0)<=windowMs).sort((a,b)=>Number(a.observedAt)-Number(b.observedAt)),first=rows[0],last=rows[rows.length-1];const make=side=>{const f=first?.stats?.[side]||{},l=last?.stats?.[side]||latest?.[side]||{},shotOnTarget=first&&last&&first!==last&&finite(f.shotOnTarget)!==null&&finite(l.shotOnTarget)!==null?Math.max(0,finite(l.shotOnTarget)-finite(f.shotOnTarget)):null,shotOff=first&&last&&first!==last&&finite(f.shotOff)!==null&&finite(l.shotOff)!==null?Math.max(0,finite(l.shotOff)-finite(f.shotOff)):null;return {shotOnTarget,shotOff,possessionPct:finite(l.possessionPct)}};return {home:make('home'),away:make('away'),from:first?.minute??null,to:last?.minute??market?.fixture?.minute??null}}
function sideEvidence(football,stats,side){const home=side==='HOME',api=home?stats.home:stats.away;return {shotOnTarget:finite(api?.shotOnTarget),shotOff:finite(api?.shotOff),corner:home?finite(football?.hCorner):finite(football?.aCorner),dangerousAttackPct:home?finite(football?.hDangerousPct):finite(football?.aDangerousPct),attackPct:home?finite(football?.hAttackPct):finite(football?.aAttackPct),possessionPct:finite(api?.possessionPct)}}
const EVIDENCE_KEYS=['shotOnTarget','shotOff','corner','dangerousAttackPct','attackPct','possessionPct'];
function evaluateEvidence(values,cfg,direction){const checks={};let passCount=0;for(const key of EVIDENCE_KEYS){const value=finite(values?.[key]),limit=finite(cfg?.[key]),pass=value!==null&&limit!==null&&(direction==='MAX'?value<=limit:value>=limit);checks[key]={value,limit,pass};if(pass)passCount++}return {values,checks,passCount,required:Number(cfg?.evidenceRequired)||1,pass:passCount>=Number(cfg?.evidenceRequired||1)}}
function minutePass(minute,cfg){const n=finite(minute);return n!==null&&n>=Number(cfg.minuteFrom)&&n<=Number(cfg.minuteTo)}
function oddsOk(value,min){const n=finite(value),m=finite(min);return n!==null&&m!==null&&n>=m&&n<=100}
function lineOk(value,min){const n=finite(value),m=finite(min);return n!==null&&m!==null&&n>=m&&n<=20&&Number.isInteger(n*4)}
function selectedOddsOne(market,pick){return finite(pick==='HOME'?market?.oneXtwo?.home:market?.oneXtwo?.away)}
function chooseOneXtwo(pred,cfg){const mode=String(cfg?.sideMode||'BOTH').toUpperCase();if(mode==='HOME')return 'HOME';if(mode==='AWAY')return 'AWAY';return Number(pred?.home||0)>=Number(pred?.away||0)?'HOME':'AWAY'}
function trailingBy(score,pick){const h=finite(score?.[0]),a=finite(score?.[1]);if(h===null||a===null)return null;return pick==='HOME'?Math.max(0,a-h):Math.max(0,h-a)}
function marketGate(name,r,row,snapshot){const run=Boolean(snapshot?.run?.[name]),cfg=snapshot?.settings?.[name]||null,m=r?.m||{},market=row?.market||{},pred=row?.prediction||{},reasons=[];if(!run||!cfg)return {market:name,pass:false,disabled:true,reasons:['ตลาดยังไม่ได้ RUN']};const minute=finite(m.minute);if(!minutePass(minute,cfg))reasons.push('อยู่นอกช่วงเวลาที่ตั้ง');const football=rollingFootball(m,cfg.rollingWindowMinutes);if(!football)reasons.push('ข้อมูลช่วงย้อนหลังยังไม่พร้อม');const stats=candidateRollingStats(String(m.id),market,cfg.rollingWindowMinutes),homeEv=sideEvidence(football,stats,'HOME'),awayEv=sideEvidence(football,stats,'AWAY');let evidence=null,pick=null,odds=null,line=null,probability=null;if(name==='oneXtwo'){pick=chooseOneXtwo(pred.oneXtwo,cfg);odds=selectedOddsOne(market,pick);probability=finite(pick==='HOME'?pred?.oneXtwo?.home:pred?.oneXtwo?.away);if(!oddsOk(odds,cfg.oddsMin))reasons.push('ราคา odds ต่ำกว่าที่ตั้ง');const gap=trailingBy(m.score,pick);if(gap===null||gap>Number(cfg.scoreTrailingMax))reasons.push('ระยะห่างของสกอร์เกินที่ตั้ง');const sideMode=String(cfg.sideMode||'BOTH').toUpperCase();if(sideMode!=='BOTH'&&sideMode!==pick)reasons.push('ฝั่งที่เลือกไม่ตรงกับ Home / Away ที่ตั้ง');evidence=evaluateEvidence(pick==='HOME'?homeEv:awayEv,cfg,'MIN');if(!evidence.pass)reasons.push(`หลักฐานผ่าน ${evidence.passCount}/${evidence.required}`)}else if(name==='over'){pick='OVER';line=finite(market?.totals?.line);odds=finite(market?.totals?.over);probability=finite(pred?.totals?.over);if(!lineOk(line,cfg.lineMin))reasons.push('เส้น Over ต่ำกว่าที่ตั้ง');if(!oddsOk(odds,cfg.oddsMin))reasons.push('ราคา odds ต่ำกว่าที่ตั้ง');const home=evaluateEvidence(homeEv,cfg,'MIN'),away=evaluateEvidence(awayEv,cfg,'MIN'),passSides=[['HOME',home],['AWAY',away]].filter(([,e])=>e.pass).sort((a,b)=>b[1].passCount-a[1].passCount);evidence={mode:'EITHER',sides:{HOME:home,AWAY:away},passingSides:passSides.map(x=>x[0]),selectedSide:passSides[0]?.[0]||null,pass:passSides.length>0};if(!evidence.pass)reasons.push('ยังไม่มีฝั่งใดผ่านหลักฐานที่ตั้ง')}else if(name==='under'){pick='UNDER';line=finite(market?.totals?.line);odds=finite(market?.totals?.under);probability=finite(pred?.totals?.under);if(!lineOk(line,cfg.lineMin))reasons.push('เส้น Under ต่ำกว่าที่ตั้ง');if(!oddsOk(odds,cfg.oddsMin))reasons.push('ราคา odds ต่ำกว่าที่ตั้ง');const home=evaluateEvidence(homeEv,cfg,'MAX'),away=evaluateEvidence(awayEv,cfg,'MAX'),mode=String(cfg.sideMode||'BOTH').toUpperCase(),pass=mode==='HOME'?home.pass:mode==='AWAY'?away.pass:home.pass&&away.pass;evidence={mode,sides:{HOME:home,AWAY:away},passingSides:[home.pass?'HOME':null,away.pass?'AWAY':null].filter(Boolean),selectedSide:mode,pass};if(!pass)reasons.push('หลักฐานเกมเงียบยังไม่ผ่านตามฝั่งที่ตั้ง')}return {market:name,pass:reasons.length===0,disabled:false,reasons,minute,football,stats,evidence,pick,line,odds,probability,settings:{...cfg}}}
function buildSignals(r,row,snapshot){const gates={oneXtwo:marketGate('oneXtwo',r,row,snapshot),over:marketGate('over',r,row,snapshot),under:marketGate('under',r,row,snapshot)},signals=[];if(gates.oneXtwo.pass)signals.push({market:'1X2',pick:gates.oneXtwo.pick,odds:gates.oneXtwo.odds,probability:gates.oneXtwo.probability,home:finite(row.prediction?.oneXtwo?.home),away:finite(row.prediction?.oneXtwo?.away),gate:gates.oneXtwo,settings:gates.oneXtwo.settings});const overPass=gates.over.pass,underPass=gates.under.pass;let totalsGate=null;if(overPass&&underPass)totalsGate=String(row.prediction?.totals?.pick||'').toUpperCase()==='UNDER'?gates.under:gates.over;else if(overPass)totalsGate=gates.over;else if(underPass)totalsGate=gates.under;if(totalsGate)signals.push({market:totalsGate.pick,pick:totalsGate.pick,line:totalsGate.line,odds:totalsGate.odds,probability:totalsGate.probability,over:finite(row.prediction?.totals?.over),under:finite(row.prediction?.totals?.under),gate:totalsGate,settings:totalsGate.settings});return {signals,gates}}
function fmt(v){const n=finite(v);return n===null?'—':n.toFixed(2)}
function panelHtml(row){const canonical=row.ledgerRecord||null,signals=Array.isArray(canonical?.signals)?canonical.signals:row.signals||[],cards=signals.map(s=>s.market==='1X2'?`<article><span>1X2</span><strong>${esc(s.pick)}</strong><small>HOME ${esc(Math.round(s.home??0))}% · AWAY ${esc(Math.round(s.away??0))}%</small><div>@ ${esc(fmt(s.odds))}</div></article>`:`<article><span>${esc(s.market)} ${esc(s.line??'—')}</span><strong>${esc(s.pick)}</strong><small>OVER ${esc(Math.round(s.over??0))}% · UNDER ${esc(Math.round(s.under??0))}%</small><div>@ ${esc(fmt(s.odds))}</div></article>`).join('');return `<section class="api-football-candidate-card"><div class="afc-head"><div><span>3.42 · MARKET SETTINGS V3</span><small>${esc(signals.map(s=>s.market).join(' + ')||'WAIT')}</small></div><strong>${row.ledgerStatus==='SYNCED'?'PREDICTION LOCKED':'SAVING PREDICTION'}</strong></div><div class="afc-grid">${cards}</div><div class="afc-foot">ตั้งค่าตลาดแยกสาย · ล็อคครั้งเดียวสำหรับคู่นี้</div></section>`}
function remove(card){card?.querySelectorAll('.api-football-candidate-card,.api-candidate-mini').forEach(n=>n.remove())}
function hydrateCard(card,row){if(!card||!row)return;remove(card);const badge=document.createElement('span');badge.className='api-candidate-mini';badge.textContent=row.ledgerStatus==='SYNCED'?'SIGNAL LOCK':'SIGNAL SAVING';card.querySelector('.card-topline')?.appendChild(badge);if(!card.classList.contains('expanded'))return;const details=card.querySelector('.event-details');if(!details)return;const wrap=document.createElement('div');wrap.innerHTML=panelHtml(row);const node=wrap.firstElementChild;if(!node)return;const predictionNode=details.querySelector(':scope > .nomad-live-prediction');if(predictionNode)predictionNode.after(node);else details.prepend(node)}
function hydrateAll(){const store=load();document.querySelectorAll('.event-compact').forEach(card=>{const row=store[String(card.dataset.matchId)];if(row?.status==='PREDICTED')hydrateCard(card,row)})}
function resultById(){const rows=window.__nomad342EventResults;return new Map(Array.isArray(rows)?rows.map(r=>[String(r?.m?.id),r]):[])}
async function fetchCandidate(r){const base=String(runtime.base||'').replace(/\/$/,'');if(!base)throw new Error('market_base_missing');const m=r.m,url=new URL(`${base}/candidate`);url.searchParams.set('home',m.home||'');url.searchParams.set('away',m.away||'');url.searchParams.set('minute',String(m.minute??''));if(Array.isArray(m.score)){url.searchParams.set('scoreHome',String(m.score[0]??''));url.searchParams.set('scoreAway',String(m.score[1]??''))}const ac=new AbortController(),timer=setTimeout(()=>ac.abort(),7000);try{const response=await fetch(url.toString(),{cache:'no-store',signal:ac.signal});let data={};try{data=await response.json()}catch{}if(!response.ok||!data?.ok)throw Object.assign(new Error(data?.error||`candidate_http_${response.status}`),{data});return data}finally{clearTimeout(timer)}}
function ledgerPayload(id,r,row){if(row?.lockPayload)return row.lockPayload;const m=r?.m||{},market=row?.market||{};return {schemaVersion:3,capturedAt:row.savedAt||Date.now(),matchId:String(id),fixtureId:String(market?.fixture?.id||''),league:m.league||'',home:m.home||'',away:m.away||'',minute:m.minute,entryScore:m.score,settingsVersion:settingsApi?.version||'market-settings-v3',signals:row.signals||[],market:{provider:market.provider||'API-Football',observedAt:market.observedAt||Date.now(),fixture:market.fixture||null,oneXtwo:market.oneXtwo||null,totals:market.totals||null,statistics:market.statistics||null}}}
function postLedger(payload){const id=String(payload?.matchId||'');if(!id)return false;const outbox=loadOutbox(),existing=outbox[id];outbox[id]={payload,createdAt:existing?.createdAt||Date.now(),attempts:existing?.attempts||0,nextAt:0,lastError:null};if(!saveOutbox(outbox))throw new Error('ledger_storage_unavailable');setLedgerStatus(id,'PENDING',{ledgerQueuedAt:Date.now()});setTimeout(flushLedgerOutbox,0);return true}
async function sendLedger(payload){const base=String(ledgerRuntime.base||'').replace(/\/$/,'');if(!base||!payload?.matchId)throw new Error('ledger_target_missing');const ac=new AbortController(),timer=setTimeout(()=>ac.abort(),Number(ledgerRuntime.timeoutMs)||6500);try{const response=await fetch(`${base}${ledgerRuntime.lockPath||'/lock'}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload),cache:'no-store',signal:ac.signal});let data={};try{data=await response.json()}catch{}if(!response.ok||data?.ok===false||data?.locked!==true)throw new Error([data?.error||`ledger_http_${response.status}`,...(data?.fields||[])].join(': '));return data}finally{clearTimeout(timer)}}
async function flushLedgerOutbox(){if(ledgerBusy)return;const outbox=loadOutbox(),now=Date.now(),next=Object.entries(outbox).filter(([,item])=>item?.payload&&Number(item.nextAt||0)<=now).sort((a,b)=>Number(a[1]?.createdAt||0)-Number(b[1]?.createdAt||0))[0];if(!next)return;ledgerBusy=true;const [id,item]=next;try{const ack=await sendLedger(item.payload),fresh=loadOutbox();delete fresh[id];saveOutbox(fresh);setLedgerStatus(id,'SYNCED',{ledgerSyncedAt:Date.now(),ledgerDuplicate:ack?.duplicate===true,ledgerRecord:ack?.record||null});hydrateAll();document.dispatchEvent(new CustomEvent('nomad342:ledgerlocked',{detail:{matchId:id,duplicate:ack?.duplicate===true}}))}catch(error){const fresh=loadOutbox(),current=fresh[id]||item,attempts=Number(current.attempts||0)+1,delay=LEDGER_RETRY_DELAYS[Math.min(attempts,LEDGER_RETRY_DELAYS.length-1)];fresh[id]={...current,attempts,nextAt:Date.now()+delay,lastError:String(error?.message||error),lastAttemptAt:Date.now()};saveOutbox(fresh);setLedgerStatus(id,'PENDING',{ledgerLastError:String(error?.message||error),ledgerLastAttemptAt:Date.now()})}finally{ledgerBusy=false;setTimeout(flushLedgerOutbox,250)}}
function recoverLockedRows(){const store=load(),outbox=loadOutbox();for(const [id,row] of Object.entries(store)){if(row?.status!=='PREDICTED'||row?.ledgerStatus==='SYNCED'||outbox[id])continue;if(row.lockPayload)postLedger(row.lockPayload)}}
function scheduleFailure(id,error){const current=state.get(id)||{attempts:0,nextAt:0},attempts=current.attempts+1;state.set(id,{attempts,nextAt:attempts>=MAX_ATTEMPTS?Infinity:Date.now()+RETRY_DELAYS[Math.min(attempts,RETRY_DELAYS.length-1)],error:String(error?.message||error)})}
async function runOne(id,r){busy=true;try{const market=await fetchCandidate(r);if(!market.oneXtwo||!market.totals||!market.statistics)throw new Error('markets_or_statistics_incomplete');const row={market,prediction:prediction(r,market)},snapshot=runningSnapshot(),evaluated=buildSignals(r,row,snapshot);row.signals=evaluated.signals;row.gates=evaluated.gates;if(!row.signals.length){state.set(id,{attempts:0,nextAt:Date.now()+GATE_RECHECK_MS,gates:evaluated.gates});return}row.settingsSnapshot=snapshot;row.lockPayload=ledgerPayload(id,r,row);remember(id,row);state.set(id,{attempts:0,nextAt:Infinity,done:true,gates:evaluated.gates});hydrateAll();postLedger(ledgerPayload(id,r,row))}catch(error){scheduleFailure(id,error)}finally{busy=false;setTimeout(scan,900)}}
function eligibleByTime(r,snapshot){const minute=finite(r?.m?.minute);if(minute===null)return false;for(const name of ['oneXtwo','over','under']){if(!snapshot?.run?.[name])continue;const c=snapshot?.settings?.[name];if(c&&minute>=Number(c.minuteFrom)&&minute<=Number(c.minuteTo))return true}return false}
function scan(){hydrateAll();recoverLockedRows();flushLedgerOutbox();if(busy)return;const snapshot=runningSnapshot();if(!snapshot?.run||!Object.values(snapshot.run).some(Boolean))return;const byId=resultById(),now=Date.now();for(const [id,r] of byId){if(!r?.m?.event?.snapshots||lockedFor(id)||!eligibleByTime(r,snapshot))continue;const s=state.get(id)||{attempts:0,nextAt:0};if(s.attempts>=MAX_ATTEMPTS||now<s.nextAt)continue;runOne(id,r);return}}
function start(){if(document.body?.dataset?.page!=='live')return;const list=document.getElementById('matchList');if(!list)return;let queued=false;const queue=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;scan()})};new MutationObserver(queue).observe(list,{childList:true});list.addEventListener('click',()=>setTimeout(hydrateAll,0));setInterval(scan,15000);setInterval(flushLedgerOutbox,15000);window.addEventListener('storage',event=>{if([settingsApi?.activeKey,settingsApi?.runKey].includes(event.key)){state.clear();queue()}});document.addEventListener('nomad342:marketsettingsrun',()=>{state.clear();queue()});document.addEventListener('nomad342:marketsettingsstop',()=>{state.clear();queue()});queue();flushLedgerOutbox();window.__nomad342ApiFootballCandidate={scan,storeKey:STORE_KEY,outboxKey:LEDGER_OUTBOX_KEY,statHistoryKey:STAT_HISTORY_KEY,footballHistoryKey:FOOTBALL_HISTORY_KEY,maxAttempts:MAX_ATTEMPTS,flushLedgerOutbox,rollingFootball,marketGate,buildSignals}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
